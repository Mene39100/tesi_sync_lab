# Topologia T2: rete multilivello con bridge temporaneo per installazione pacchetti

servergm[0]=A
boundary[0]=A
boundary[1]=B
clientchrony[0]=A
clientntp[0]=B
clientptp[0]=B

# Bridge temporaneo per ogni nodo (accesso a Internet solo durante setup)
servergm[bridged]=true
boundary[bridged]=true
clientchrony[bridged]=true
clientntp[bridged]=true
clientptp[bridged]=true
