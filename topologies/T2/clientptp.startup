#!/bin/bash
set -e
ip addr add 10.0.20.3/24 dev eth0
ip link set eth0 up
echo "nameserver 8.8.8.8" > /etc/resolv.conf

apt update -y
apt install -y linuxptp

# Configurazione PTP (client)
cat > /etc/ptp/client_ptp.conf << 'EOF'
[global]
twoStepFlag 1
domainNumber 24
delay_mechanism E2E
[eth0]
EOF
